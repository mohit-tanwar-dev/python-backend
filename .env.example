# Sample .env file — copy to `.env` and edit as needed.
# All variables are optional — the service runs with sensible defaults.

APP_NAME=python-backend
APP_ENV=local
APP_DEBUG=true
APP_HOST=0.0.0.0
APP_PORT=8000
APP_LOG_LEVEL=INFO
